import React from 'react';


// add this to weather project
<TouchableOpacity style={styles.buttonStyle} onPress={() => navigate('Settings')}>
    <Text style={styles.buttonText}>Settings</Text>
</TouchableOpacity>

export default Settings;
